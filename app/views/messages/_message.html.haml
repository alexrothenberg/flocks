.entry
  .entry-message= auto_link(message.message)
  .entry-nickname=message.nickname.name
  .date= message.created_at 
  .entry-action= link_to('Delete', message_url(message), :method => :delete, :confirm => "Are you sure you want to delete #{message.nickname}'s message")
